import React from 'react'

type Props = {
  setCurrentPage: any
}

export const Boost = ({ setCurrentPage }: Props) => {
  return (
    <div className='container-wrapper'>
      Boost
    </div>
  )
}
