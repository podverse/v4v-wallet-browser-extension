import React from 'react'

type Props = {
  setCurrentPage: any
}

export const Settings = ({ setCurrentPage }: Props) => {
  return (
    <div className='container-wrapper'>
      Settings
    </div>
  )
}
