import React from 'react'

type Props = {
  setCurrentPage: any
}

export const Dashboard = ({ setCurrentPage }: Props) => {
  return (
    <div className='container-wrapper'>
      Dashboard
    </div>
  )
}
