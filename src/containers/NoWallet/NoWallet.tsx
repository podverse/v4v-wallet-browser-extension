import React from 'react'

type Props = {
  setCurrentPage: any
}

export const NoWallet = ({ setCurrentPage }: Props) => {
  return (
    <div className='no-wallet container-wrapper'>
      No Wallet
    </div>
  )
}
